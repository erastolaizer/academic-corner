import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CollMaterialPage } from './coll-material';

@NgModule({
  declarations: [
    CollMaterialPage,
  ],
  imports: [
    IonicPageModule.forChild(CollMaterialPage),
  ],
})
export class CollMaterialPageModule {}
